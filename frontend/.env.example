# Frontend environment variables — NEVER commit this file to git
# Copy to .env.local and fill in your Render backend URL

# URL of the Render backend (no trailing slash)
# Leave empty for local dev (Vite dev proxy handles it automatically)
VITE_API_BASE_URL=https://revguard-api.onrender.com
